# Agent Workflow Engine

Minimal agent workflow engine with a code-review example, a FastAPI REST API, and safe AST-based condition evaluation.

This `README.md` is a concise quick-start. For full architecture, examples, security notes and roadmap, see `docs/README_FULL.md` (or `README_FULL.md`).

---

## Quickstart (recommended)

### Prerequisites
- Python 3.9+ (3.11 recommended)
- `git` (optional)
- pip (comes with Python)

### Create & activate virtual environment

**Windows (PowerShell)**
```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
